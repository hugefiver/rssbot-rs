[package]
name = "rssbot"
version = "2.0.1"
authors = ["iovxw <iovxw@outlook.com>"]
edition = "2024"

[features]
default = ["rustls"]

# enable this to set a subscription limit
hosted-by-iovxw = ["reqwest/trust-dns"]
native-tls = [
    # "tbot/default",
    "teloxide/native-tls",
    "hyper-proxy/default",
    "reqwest/default",
    "reqwest/native-tls-vendored",
    "reqwest/native-tls-alpn",
]
rustls = [
    # "tbot/rustls",
    "teloxide/rustls",
    "hyper-proxy/rustls",
    "reqwest/rustls-tls",
]

[package.metadata.cross.build.env]
passthrough = ["LOCALE"]

[build-dependencies]
ctl10n = "0.2.0"

[dependencies]
anyhow = { version = "1.0.100", features = ["backtrace"] }
futures = "0.3.31"
tokio = { version = "1.48.0", features = ["full"] }
tokio-util = { version = "0.7.18", features = ["full"] }
tokio-stream = "0.1.18"
thiserror = "2.0.18"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
regex = "1.12.2"
quick-xml = { version = "0.39.0", features = ["encoding", "escape-html"] }
chrono = "0.4"
pinyin = "0.11"
either = "1.15"
atomicwrites = "0.4"
clap = { version = "4.5.56", features = ["derive"] }
teloxide = { version = "0.17.0", features = ["macros", "ctrlc_handler"], default-features = false }

# [dependencies.tbot]
# version = "0.6"
# git = "https://gitlab.com/SnejUgal/tbot.git"
# default-features = false
# features = ["proxy"]

[dependencies.hyper-proxy]
version = "0.9"
default-features = false

[dependencies.reqwest]
version = "0.12"
default-features = false
features = ["gzip", "json"]
